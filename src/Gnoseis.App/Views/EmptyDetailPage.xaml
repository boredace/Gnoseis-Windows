<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Gnoseis.App.Views.EmptyDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Shown in a section's detail pane when no record is selected. -->
    <StackPanel
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="16">
        <Border
            x:Name="Tile"
            Width="72"
            Height="72"
            HorizontalAlignment="Center"
            CornerRadius="16"
            Opacity="0.85">
            <FontIcon x:Name="TileIcon" FontSize="32" Foreground="White" />
        </Border>
        <TextBlock
            x:Name="MessageText"
            HorizontalAlignment="Center"
            Style="{StaticResource SubtitleTextBlockStyle}"
            TextAlignment="Center"
            TextWrapping="Wrap" />
        <TextBlock
            x:Name="HintText"
            HorizontalAlignment="Center"
            Style="{StaticResource SecondaryTextStyle}"
            TextAlignment="Center" />
    </StackPanel>
</Page>
